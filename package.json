{
  "name": "nimbusfs-root",
  "private": true,
  "scripts": {
    "dev": "echo 'To run everything at once, please use ./run.bat or run dev:backend and dev:frontend in separate terminals.'",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:nodes": "npm run node --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.8",
    "tailwindcss": "^4.2.1"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "clsx": "^2.1.1",
    "framer-motion": "^12.35.1",
    "lucide-react": "^0.577.0",
    "tailwind-merge": "^3.5.0"
  }
}
